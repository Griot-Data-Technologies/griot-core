## Auto-generated GitHub Actions workflow for {{ contract_id }}
## Generated by Griot scaffold on {{ generated_at }}

name: "{{ contract_id }} - Contract Validation"

on:
  push:
    paths:
      - "contracts/{{ contract_id }}.yaml"
      - "contracts/{{ contract_id }}.yml"
      - "griot.lock"
  pull_request:
    paths:
      - "contracts/{{ contract_id }}.yaml"
      - "contracts/{{ contract_id }}.yml"
      - "griot.lock"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Griot CLI
        run: pip install griot-cli griot-core

      - name: Verify lock file
        run: griot verify-lock

      - name: Lint contract
        run: griot lint contracts/{{ contract_id }}.yaml

      - name: Validate contract structure
        run: griot validate contracts/{{ contract_id }}.yaml
